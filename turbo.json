{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "icons/**", "scripts/**", "manifest.json", "package.json"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": [],
      "inputs": ["src/**", "**/*.ts", "**/*.tsx"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "**/*.test.ts", "**/*.test.tsx"],
      "outputs": ["coverage/**"]
    }
  }
}
